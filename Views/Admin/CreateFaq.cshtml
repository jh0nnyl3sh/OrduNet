@model OrduNet.Web.Models.Entities.HelpdeskFaq
@{
    ViewData["Title"] = "Yeni SSS Ekle";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4">
                <div class="card-header bg-white border-bottom py-3">
                    <h5 class="fw-bold text-dark mb-0"><i class="bi bi-plus-circle text-primary me-2"></i> Yeni Soru (Hızlı Yardım) Ekle</h5>
                </div>
                <div class="card-body p-4">
                    <form asp-action="CreateFaq" method="post">
                        <div class="mb-3">
                            <label asp-for="Question" class="form-label fw-bold">Soru / Konu</label>
                            <input asp-for="Question" class="form-control" placeholder="Örn: E-imza çalışmıyor" required />
                            <span asp-validation-for="Question" class="text-danger small"></span>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Answer" class="form-label fw-bold">çözüm / Cevap</label>
                            <textarea asp-for="Answer" class="form-control" rows="3" placeholder="Örn: Akis kart izleme aracçnç..." required></textarea>
                            <span asp-validation-for="Answer" class="text-danger small"></span>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label asp-for="IconClass" class="form-label fw-bold">Bootstrap ikon Sınıfç</label>
                                <input asp-for="IconClass" class="form-control" placeholder="Örn: bi-usb-drive" />
                                <div class="form-text small">Bootstrap Icons sitesinden (çr: bi-printer) bulabilirsiniz.</div>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="DisplayOrder" class="form-label fw-bold">Göruntülenme Sırasç</label>
                                <input asp-for="DisplayOrder" type="number" class="form-control" value="0" />
                            </div>
                        </div>

                        <div class="mb-4 form-check form-switch">
                            <input asp-for="IsActive" class="form-check-input" type="checkbox" role="switch" checked />
                            <label asp-for="IsActive" class="form-check-label fw-bold">Aktif (Yayında)</label>
                        </div>

                        <div class="d-flex justify-content-end gap-2">
                            <a asp-action="HelpdeskFaqs" class="btn btn-light">İptal</a>
                            <button type="submit" class="btn btn-primary fw-bold"><i class="bi bi-check-circle me-1"></i> Kaydet</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
